name: beszel
server: hermes.haloy.dev
image:
  repository: "henrygd/beszel:latest"
  history:
    strategy: none
deployment_strategy: replace
domains:
  - domain: beszel.haloy.dev
port: 8090
volumes:
  - beszel_data:/beszel_data
  - beszel_socket:/beszel_socket

